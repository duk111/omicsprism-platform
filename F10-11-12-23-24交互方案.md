# F10 / F11 / F12 / F23 / F24 动态交互页面设计方案

> 本方案基于 `omicsprism/src/omicsprism/visualization/static/` 下的静态绘图逻辑，为每张图设计贴近静态图视觉效果的交互方案。所有交互图统一支持：**滚轮放大缩小**、**按住鼠标拖动平移**。

---

## 一、总体设计原则

| 原则 | 说明 |
|------|------|
| **视觉还原** | 颜色映射、排序规则、标签方向、模块色条等尽量与静态图一致 |
| **交互统一** | 所有图均支持滚轮缩放（wheel zoom）和拖拽平移（drag pan），参考 `FigureViewer` 的交互模式 |
| **状态可切换** | F11/F24、F12/F23 共用同一个交互页面，通过侧栏下拉框在 Gene 视图与 Module 视图之间切换 |
| **初始态贴近静态图** | 页面加载时的默认状态（如排序、颜色方案、显著性阈值）与对应静态图保持一致 |
| **生信可读** | 每张图提供简介和选取规则说明，帮助生信用户理解图中展示的内容 |

**通用交互实现方式建议**：
- 若使用 **SVG 自绘**（推荐 F10、F11/F24）：通过 CSS `transform: scale()` 实现缩放，通过修改 `scrollLeft/scrollTop` 或 `transform: translate()` 实现拖拽平移。
- 若使用 **Plotly**（推荐 F12/F23）：开启 `config: { scrollZoom: true, displayModeBar: true }`，并在外层容器上阻止 wheel 事件冒泡（已修复）。Plotly 内置的 drag 模式设为 `"pan"`。

---

## 二、F10 — UpSet Plot（Association Evidence Overlap）

### 2.1 图简介（生信可读）

UpSet 图用于展示**基因-代谢物关联候选边**在 5 种不同证据来源之间的**重叠模式**。pipeline 中的每一条 metabolite-gene 边（edge）会经过 5 种方法检验：

| 证据 | 方法说明 |
|------|---------|
| **PCC** | Pearson 相关系数筛选 |
| **Spearman** | Spearman 秩相关系数筛选 |
| **MI** | 互信息（Mutual Information）筛选 |
| **ElasticNet** | 弹性网络正则化回归特征选择 |
| **XGBoost** | XGBoost 特征重要性选择 |

只要一条边被至少一种方法选中，就进入统计。UpSet 图回答的问题是：**有多少边同时被多种方法共同支持？** 重叠度越高，关联的可信度越强。

### 2.2 选取规则

1. 从 `gene_scores_df` 中提取所有 metabolite-gene 边及其 5 个证据布尔列。
2. 对同一 metabolite-gene 组合的去重边，证据列取**最大值**（即该边只要有一次被某方法选中，就认为有该证据）。
3. 按证据列的组合模式（如 {PCC, Spearman}、{MI, XGBoost} 等）分组计数，得到每种交集模式的边数量（Count）。
4. 仅展示 **Top 30** 交集模式，排序优先级：
   - ① Count（边数量）降序
   - ② SupportCount（该交集涉及的方法数）降序
   - ③ Pattern（二进制模式字符串）升序

### 2.3 静态图布局分析

静态图采用 4 宫格 `gridspec` 布局：

```
┌─────────────┬─────────────────────────────┐
│  Summary    │  Bar Chart (Intersection    │
│  Text       │  Size = Count)              │
├─────────────┼─────────────────────────────┤
│  Set Size   │  Matrix (Active dots)       │
│  Horizontal │  + Connecting lines         │
│  Bar        │                             │
└─────────────┴─────────────────────────────┘
```

- **Bar Chart**：每个交集的候选边数量，纵轴为 "Candidate edges"。
- **Set Size Bar**：左下角横向条形图，展示 5 种证据各自独立选中的边总数。
- **Matrix**：右下角矩阵，每列代表一个交集模式；有颜色的圆点表示该交集包含哪些证据，竖线连接同一列中的活跃证据。
- **Summary**：左上角文字，显示 evidence-positive edges 总数和展示交集数。

### 2.4 交互设计方案

| 交互项 | 设计 |
|--------|------|
| **滚轮缩放** | 作用于整个 SVG 画布，通过 `transform: scale()` 缩放，缩放中心为鼠标位置 |
| **拖拽平移** | 在矩阵和条形图区域按住鼠标拖动，修改容器的 `scrollLeft/scrollTop`（或 SVG 的 `translate`） |
| **悬浮提示** | 悬浮在 **Matrix 的圆点** 上时，Tooltip 显示：`{EvidenceLabel}: {Count} edges`；悬浮在 **Bar Chart 的柱子上** 时，显示该交集的完整模式及 Count |
| **排序切换** | 侧栏提供 `sort_by` 下拉框：`size`（按 Count 降序）、`degree`（按 SupportCount 降序）、`combination`（按 Pattern 升序） |
| **最大交集数** | 侧栏提供 `max_intersections` 滑块/下拉：10 / 20 / 30 / 40 / 50 |

### 2.5 初始状态

- `sort_by = "size"`
- `max_intersections = 30`
- 缩放比例 `zoom = 1`，居中显示

### 2.6 渲染工具建议

**推荐原生 SVG + React**（参考 `InteractiveDendrogram` 的实现方式）。原因：
- UpSet 的 4 宫格布局是高度定制化的，Plotly 的 subplot 难以完美复现静态图的视觉比例和模块分隔带。
- SVG 自绘可精确控制 matrix 中圆点的大小、颜色、连接线样式，以及 set size bar 的横向布局。
- 滚轮缩放和拖拽平移可直接套用 `InteractiveDendrogram` 中已有的 `handleWheel/handleMouseDown/handleMouseMove` 模式。

---

## 三、F11 / F24 — Bubble Heatmap（气泡热图）

### 3.1 图简介（生信可读）

气泡热图用于可视化**基因/模块与代谢物之间的关联网络**。每个气泡代表一对关联关系：

- **F11（Gene-Level）**：展示高置信网络中 **Top 100 基因** 与代谢物的关联。气泡越大，表示该基因-代谢物对的关联强度（`EdgeWeight`）越高；颜色越红/越蓝，表示 Spearman 相关系数越正/越负。
- **F24（Module-Level）**：展示 **WGCNA 共表达模块**（Module）与代谢物的关联。每个模块是一组协同表达的基因的集合，用模块特征基因（eigengene）代表整个模块的表达模式。气泡越大，表示模块-代谢物关联的统计显著性越高（`-log10(FDR)`）；颜色表示相关方向。

**生信背景补充**：WGCNA（Weighted Gene Co-expression Network Analysis）将基因聚类为模块，grey module 表示未被归类的基因，通常不纳入后续分析。

### 3.2 选取规则

#### F11 — Gene Bubble Heatmap

1. **数据来源**：`high_confidence_network_df`（高置信关联网络）。
2. **Gene 选取（Top 100）**：
   - 先对每个基因取所有代谢物中最大的 `EdgeWeight`；
   - 按该最大值降序取前 100 个基因；
   - 再按模块顺序 → 模块内按 `EdgeWeight` 降序排列，形成最终的 y 轴顺序。
3. **Metabolite 选取**：
   - 按代谢物关联的最大 `EdgeWeight` 降序；
   - 若相同，按关联基因数（`EdgeCount`）降序。
4. **气泡大小**：映射 `EdgeWeight`，范围 `min_size=18` ~ `max_size=190`。
5. **气泡颜色**：映射 `SpearmanRho`，色图 `RdBu_r`，范围 `[-1, 1]`。
6. **左侧色条**：每个基因所属的模块颜色（module color strip）。
7. **模块分隔线**：不同模块的基因之间绘制水平灰线。

#### F24 — Module Bubble Plot

1. **数据来源**：`module_metabolite_assoc_df`（模块-代谢物关联表）。
2. **排除 grey module**。
3. **Module 选取**：按模块预设顺序排列；若不在预设中，则按显著性（FDR）和 `AbsRho` 排序。
4. **Metabolite 选取**：按 `AbsRho` 最大值降序排列。
5. **气泡大小**：映射 `-log10(FDR)`，范围 `24` ~ `234`；若 FDR 无效，则回退到 `AbsRho`。
6. **气泡颜色**：映射 `SpearmanRho`，色图 `RdBu_r`，范围 `[-1, 1]`。

### 3.3 视觉设计要点（贴近静态图）

| 元素 | F11 设计 | F24 设计 |
|------|---------|---------|
| **左侧标签栏** | Gene 名称列表 + module color strip（窄条） | Module 名称列表（无 color strip，因为 y 轴本身就是 module） |
| **模块分隔线** | 不同 module 的基因之间画灰线 | 不需要 |
| **气泡描边** | `#111827`，线宽 `0.35` | `#111827`，线宽 `0.35` |
| **网格线** | 浅灰色 `#e5e7eb` | 浅灰色 `#e5e7eb` |
| **Colorbar** | 标签 "Spearman rho" | 标签 "Spearman rho" |
| **Legend** | EdgeWeight 大小图例（3 个参考尺寸） | 不需要（或加 FDR 图例） |

### 3.4 交互设计方案

| 交互项 | 设计 |
|--------|------|
| **滚轮缩放** | 整个图表区域支持缩放，缩放中心为鼠标位置 |
| **拖拽平移** | 按住鼠标拖动平移视图；当图表尺寸超过容器时出现滚动条 |
| **悬浮提示** | 悬浮在气泡上时，Tooltip 显示完整信息： |
| | **F11**：`Gene: {gene}\nMetabolite: {metabolite}\nSpearman ρ: {rho:.3f}\nEdgeWeight: {ew:.3f}\nModule: {module}` |
| | **F24**：`Module: {module}\nMetabolite: {metabolite}\nSpearman ρ: {rho:.3f}\n-log10(FDR): {neglogfdr:.2f}` |
| **视图切换** | 侧栏 `Level` 下拉框：`Gene` / `Module`，切换时整个图表重新渲染（数据、排序、轴标签均变化） |
| **Rho 阈值过滤** | 侧栏提供 `Min |ρ|` 滑块（0.0 ~ 1.0，步长 0.05），隐藏绝对值低于阈值的气泡 |
| **排序方式** | 侧栏提供 X/Y 轴排序选项（如按名称、按最大相关值等） |

### 3.5 初始状态

- **F11 进入时**：`level = "gene"`，`min_abs_rho = 0.0`，排序与静态图一致（gene 按模块+EdgeWeight，metabolite 按 EdgeCount+EdgeWeight）
- **F24 进入时**：`level = "module"`，`min_abs_rho = 0.0`，排序与静态图一致（module 按预设顺序，metabolite 按 AbsRho）

### 3.6 渲染工具建议

**推荐原生 SVG + React** 或 **D3.js**。原因：
- 需要精确控制气泡的 x/y 网格定位、大小映射、左侧 color strip、模块分隔线等细节，Plotly 的 `scatter` 模式难以完美还原静态图的网格对齐和辅助元素。
- 数据量可控（F11 最多 100×N 个气泡，F24 通常为 10-20×N），SVG 渲染性能足够。
- 缩放/平移可直接复用 `InteractiveDendrogram` 的交互代码。

若工期紧张，也可使用 **Plotly scatter** 并开启 `scrollZoom`，但需要接受左侧 color strip 和模块分隔线无法精确还原的折中。

---

## 四、F12 / F23 — Correlation Heatmap（相关热图）

### 4.1 图简介（生信可读）

相关热图用颜色深浅和冷暖来展示两组变量之间的 Spearman 秩相关系数：

- **F12（Gene-Metabolite）**：直接计算**单个基因**表达谱与**单个代谢物**含量谱之间的相关性。红色表示正相关（基因高表达时代谢物也高），蓝色表示负相关（基因高表达时代谢物低）。这是最直接、最细粒度的关联视角。
- **F23（Module-Metabolite）**：计算**模块特征基因（eigengene）**与代谢物之间的相关性。模块特征基因是整个模块内所有基因表达模式的第一主成分，代表该模块的整体表达趋势。相比 F12，F23 从"单基因噪声"提升到"模块协同信号"层面，关联更稳定、更易解释。

**显著性标注**：F23 中带有星号标注（`* p<0.05, ** p<0.01, *** p<0.001`），基于 FDR 或 PValue 校正。

### 4.2 选取规则

#### F12 — Top Gene × Top Metabolite Correlation

1. **数据来源**：`gene_expression_df`（基因表达矩阵）、`metabolomics_df`（代谢组矩阵）。
2. **样本对齐**：取两组数据共有的样本（`shared_samples`），至少需要 3 个样本。
3. **Top Genes 选取**（默认 `top_n = 20`，可由 `cfg.top_key_genes_plot_n` 调整）：
   - **优先级 1**：从 `key_gene_summary_df` 中按顺序取关键基因；
   - **优先级 2**：从 `high_confidence_network_df` 中取参与高置信关联的基因；
   - **优先级 3**：从 `total_association_network_df` 中取；
   - **回退**：按基因表达方差降序取。
4. **Top Metabolites 选取**（默认 `top_m = 20`，可由 `cfg.support_plot_top_metabolites` 调整）：
   - **优先级 1**：从 `metabolite_summary` 中按顺序取；
   - **优先级 2**：从 `high_confidence_network_df` / `total_association_network_df` 中取；
   - **回退**：按代谢物方差降序取。
5. **相关计算**：对每一对 gene-metabolite，计算 Spearman 秩相关系数，组成矩阵。
6. **颜色范围**：`vlag` 色图，center=0，vmin=-vmax，vmax=max(0.25, 实际最大绝对值)。

#### F23 — Module-Metabolite Association Heatmap

1. **数据来源**：`module_metabolite_assoc_df`（模块-代谢物关联表）。
2. **排除 grey module**。
3. **显著性列**：优先使用 `FDR`；若不存在则使用 `PValue`。
4. **Module 排序**：
   - **优先级 1**：`module_summary_df` 中的模块顺序；
   - **优先级 2**：按显著性（FDR/PValue 升序）→ `AbsRho` 降序 → Module 名称升序。
5. **Metabolite 排序**：按显著性升序 → `AbsRho` 降序 → Metabolite 名称升序。
6. **颜色范围**：`vlag` 色图，center=0，vmin=-vmax，vmax=max(0.25, 实际最大绝对值)。
7. **显著性标注**：`p <= 0.05` 标 `*`，`<= 0.01` 标 `**`，`<= 0.001` 标 `***`。

### 4.3 视觉设计要点（贴近静态图）

| 元素 | F12 设计 | F23 设计 |
|------|---------|---------|
| **左侧标签栏** | Gene 名称 + module color strip | Module 名称（无 color strip） |
| **色图** | `vlag`，center=0 | `vlag`，center=0 |
| **单元格边框** | `linewidths=0.35`，`linecolor=#f3f4f6` | `linewidths=0.5`，`linecolor=#f3f4f6` |
| **显著性标注** | 无 | 星号 `*` / `**` / `***` |
| **Colorbar 标题** | "Correlation" | "Spearman rho" + 星号说明 |
| **总标题** | "Top N Genes x Top M Metabolites" | "Module-Metabolite Association Heatmap" |

### 4.4 交互设计方案

| 交互项 | 设计 |
|--------|------|
| **滚轮缩放** | Plotly 内置 `scrollZoom: true`，或外层容器监听 wheel + CSS scale |
| **拖拽平移** | Plotly 内置 `dragmode: "pan"` |
| **悬浮提示** | 悬浮在热图单元格上时，Tooltip 显示： |
| | **F12**：`Gene: {gene}\nMetabolite: {metabolite}\nSpearman ρ: {rho:.3f}` |
| | **F23**：`Module: {module}\nMetabolite: {metabolite}\nSpearman ρ: {rho:.3f}\nFDR: {fdr:.2e} ({stars})` |
| **视图切换** | 侧栏 `View` 下拉框：`gene-metabolite` / `module-metabolite`，切换后整体重绘 |
| **颜色方案** | 侧栏提供色图切换：`vlag` / `RdBu_r` / `coolwarm` / `viridis`（默认 `vlag`） |
| **显示显著性** | F23 模式下提供开关 `Show significance`，控制星号标注的显示/隐藏 |

### 4.5 初始状态

- **F12 进入时**：`view = "gene-metabolite"`，`color_scheme = "vlag"`，`show_significance = true`
- **F23 进入时**：`view = "module-metabolite"`，`color_scheme = "vlag"`，`show_significance = true`

### 4.6 渲染工具建议

**推荐 Plotly heatmap**（沿用现有 `InteractiveHeatmap` 的基础）。原因：
- 热图是 Plotly 的原生支持图表类型，`heatmap` trace 可直接接受 `z` 矩阵，`annotations` 支持文本标注（星号）。
- Plotly 原生支持 `scrollZoom` 和 `dragmode: "pan"`，无需额外编写缩放平移代码。
- F12 左侧的 module color strip 可在 Plotly 外层的左侧用独立 SVG/div 实现，与主热图联动滚动。
- 若希望左侧 color strip 也能随缩放平移联动，可将整个图表（color strip + heatmap）放入一个共同的可缩放容器中。

---

## 五、页面路由与参数映射

| 来源静态图 | 交互页面 URL | 初始参数 |
|-----------|-------------|---------|
| F10 | `/interactive/{jobId}/upset` | 无 |
| F11 | `/interactive/{jobId}/bubble-heatmap` | `?level=gene` |
| F24 | `/interactive/{jobId}/bubble-heatmap` | `?level=module` |
| F12 | `/interactive/{jobId}/corr-heatmap` | `?view=gene-metabolite` |
| F23 | `/interactive/{jobId}/corr-heatmap` | `?view=module-metabolite` |

**参数说明**：`InteractivePageShell` 已支持将 URL search params 合并为初始 state，因此上述参数会自动注入 `controls.state`，交互组件读取即可。

---

## 六、前端组件修改清单（后续实施参考）

| 页面 | 组件文件 | 待修改内容 |
|------|---------|-----------|
| F10 | `InteractiveUpSet.tsx` | 重写为原生 SVG 实现，增加滚轮缩放、拖拽平移、悬浮 Tooltip |
| F11/F24 | `InteractiveBubble.tsx` | 增强为原生 SVG 或 D3 实现，增加滚轮缩放、拖拽平移、模块色条、模块分隔线 |
| F12/F23 | `InteractiveHeatmap.tsx` | 增加左侧 module color strip（F12 模式），确认 `scrollZoom` 可用；修复 F12/F23 切换时的数据过滤逻辑 |
| 通用 | `FigureViewer.tsx` | 为 F11/F24 补充 `level` 参数，为 F12/F23 补充 `view` 参数 |

---

## 七、方案总结

| 图 | 核心信息 | 推荐渲染方式 | 关键交互 |
|---|---------|------------|---------|
| **F10 UpSet** | 5 种证据来源的重叠分析 | 原生 SVG | 缩放、平移、Matrix 圆点悬浮显示 Count |
| **F11 Bubble** | Top 100 基因 × 代谢物关联网络 | 原生 SVG / D3 | 缩放、平移、气泡悬浮显示 ρ 和 EdgeWeight、Level 切换 |
| **F24 Bubble** | 模块 × 代谢物关联网络 | 原生 SVG / D3 | 缩放、平移、气泡悬浮显示 ρ 和 -log10(FDR)、Level 切换 |
| **F12 Heatmap** | Top 基因 × Top 代谢物 Spearman 相关 | Plotly heatmap | 缩放、平移、单元格悬浮显示 ρ、View 切换、色图切换 |
| **F23 Heatmap** | 模块 × 代谢物 Spearman 相关 + 显著性 | Plotly heatmap | 缩放、平移、单元格悬浮显示 ρ 和 FDR、View 切换、星号开关 |
