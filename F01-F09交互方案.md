# OmicsPrism 可视化交互方案（F01–F09）

> 本文档描述 F01（聚类图）和 F02–F09（PCA 散点/配对图）的交互页面设计方案。
> 所有设计均基于 `omicsprism/src/omicsprism/visualization/static/pca.py` 与 `base.py` 中的静态绘图逻辑，确保交互图的视觉表现与静态图一致。

---

## 一、架构概述

### 1.1 页面路由

| 静态图 | pageId | 说明 |
|--------|--------|------|
| F01 | `dendrogram` | 聚类图，独立页面 |
| F02–F09 | `pca` | 统一 PCA 探索页面，通过 URL 参数区分初始状态 |

### 1.2 URL 参数传递（F02–F09）

`FigureViewer.tsx` 已改造为通过 URL Search Params 传递初始状态：

```
/interactive/{jobId}/pca?source=transcriptome&color_by=group1&x_pc=1&y_pc=2
```

各图的初始参数映射：

| 静态图 | source | color_by | x_pc | y_pc |
|--------|--------|----------|------|------|
| F02 | transcriptome | group1 | 1 | 2 |
| F03 | transcriptome | group2 | 1 | 2 |
| F04 | transcriptome | group1 | 1 | 2 |
| F05 | transcriptome | group2 | 1 | 2 |
| F06 | metabolome | group1 | 1 | 2 |
| F07 | metabolome | group2 | 1 | 2 |
| F08 | metabolome | group1 | 1 | 2 |
| F09 | metabolome | group2 | 1 | 2 |

`InteractivePageShell` 在 `useEffect` 中读取 URL 参数并合并到 `controls.state`，已实现。

---

## 二、F01 — 聚类图（Dendrogram）

### 2.1 静态图参考逻辑

文件：`omicsprism/src/omicsprism/visualization/static/pca.py:777–800`

```python
linkage_matrix = linkage(np.asarray(adata.X, dtype=np.float32), method="average")
color_threshold = 0.70 * float(linkage_matrix[:, 2].max())

dendrogram(
    linkage_matrix,
    labels=adata.obs_names.tolist(),
    leaf_rotation=90,
    leaf_font_size=max(4, min(9, int(fig_width * 72 / max(1, adata.n_obs) * 0.42))),
    color_threshold=color_threshold,
    above_threshold_color="#aaaaaa",
    ax=ax,
)
```

关键参数：
- **聚类方法**：`average`（UPGMA）
- **距离度量**：Euclidean（scipy 默认）
- **着色阈值**：最大节点高度的 **70%**
- **低于阈值**：自动着色（scipy 默认色谱）
- **高于阈值**：`#aaaaaa` 灰色
- **图宽**：`max(12.0, n_obs * 0.24)` 英寸
- **图高**：6 英寸
- **Y轴标签**：`Euclidean Distance`
- **X轴标签**：`Sample`

### 2.2 交互设计方案

#### 渲染方式
使用 **D3.js** 自绘制 SVG，原因：
- Plotly 对 dendrogram 的支持有限，难以做到分支级别的精细交互
- D3 可完全复刻 scipy dendrogram 的布局算法

#### 数据结构（tree_data）

后端 `export_dendrogram`（`figure_data.py:264–308`）已输出：

```json
{
  "tree_data": {
    "nodes": [
      {"id": 0, "name": "sample_001", "is_leaf": true, "height": 0.0},
      {"id": 5, "left": 0, "right": 1, "height": 0.35, "is_leaf": false, "count": 2}
    ],
    "linkage": [[0, 1, 0.35, 2], [2, 5, 0.72, 3]],
    "labels": ["sample_001", "sample_002", ...],
    "color_threshold": 0.70,
    "n_leaves": 48
  }
}
```

#### 绘制要点

1. **布局算法**
   - 叶子节点在 X 轴上等距排列，从左到右顺序与静态图一致
   - 内部节点 Y 坐标 = 其子节点 Y 坐标的平均值
   - 内部节点 X 坐标 = `pad_left + (height / max_height) * plot_width`

2. **线条绘制**
   - 内部节点画垂直线连接两个子节点
   - 从内部节点到每个子节点画水平线
   - 线条颜色：
     - 高度 ≤ 阈值：使用 scipy 默认色谱（按分支 ID hash 分配 hsl 色相）
     - 高度 > 阈值：`#aaaaaa` 灰色
   - 线条粗细：着色分支 1.5px，灰色分支 1px

3. **Y轴（纵坐标）**
   - 必须显示数值刻度，标注 Euclidean Distance
   - 刻度范围 0 ~ max_height
   - 刻度线颜色：`#cbd5e1`

4. **X轴标签（样本ID）**
   - 字体大小：随样本数动态计算，公式 `max(8, min(14, int(600 / n_leaves)))`
   - 旋转 90°（垂直）
   - 颜色：`#111827`
   - 字体：Arial, sans-serif

5. **鼠标悬停交互**
   - 悬停在**分支**上时，高亮该分支下的所有叶子节点
   - Tooltip 显示：
     - 如果是叶子分支（直接连接叶子）：显示叶子样本ID
     - 如果是内部节点分支：显示该分支下所有样本ID列表（前10个 + "… 等N个"）
   - 高亮效果：被高亮的叶子标签加粗、变色（`#dc2626`），其他分支变透明（opacity 0.3）

6. **页面尺寸**
   - 图表区域：宽度 100%（min-width 800px），高度 `max(400px, n_leaves * 14px)`
   - 整体页面无横向滚动条（SVG 使用 `viewBox` + `preserveAspectRatio="xMidYMid meet"` 自适应）
   - 右侧控制面板固定 280px 宽

7. **控制面板**
   - Color threshold 滑块（0.3 ~ 1.0，步进 0.01）
   - 阈值参考线（虚线，随滑块实时更新）
   - Orientation 切换（vertical / horizontal）

### 2.3 当前问题与修复

- **问题**：`InteractiveDendrogram.tsx` 当前使用 `requestAnimationFrame` 渲染，但 `nodes` 为空时一直显示 "Rendering dendrogram..."
- **根因**：后端 `figure_data.py` 的 `export_dendrogram` 函数依赖 `context.pca_adata`，如果 correlation 分析未正常生成 adata 或 adata.n_obs < 2，则返回 None
- **修复方向**：检查后端 `export_pca_page` 是否同时生成 dendrogram 数据，或确保 `export_dendrogram` 在 correlation 分析完成时被调用

---

## 三、F02–F09 — PCA 统一交互页面

### 3.1 静态图参考逻辑

文件：`omicsprism/src/omicsprism/visualization/static/pca.py:304–540`

#### 核心函数：`_plot_pca_from_matrix`

**分组编码规则：**

| 场景 | group1 | group2 | 颜色来源 | 形状来源 |
|------|--------|--------|----------|----------|
| 无 group2（F02, F06） | 颜色+形状 | — | `_group_color_map(group1)` | `_group_marker_map(group1)` |
| 有 group2（F03, F05, F07, F09） | 形状 | 颜色 | `_global_secondary_group_color_map(group2)` | `_group_marker_map(group1)` |

**颜色映射：**

- **group1 颜色**（`base.py:33–46`）：
  ```python
  PCA_GROUP_PALETTE = [
      "#1f77b4", "#d62728", "#2ca02c", "#9467bd", "#ff7f0e", "#17becf",
      "#8c564b", "#e377c2", "#7f7f7f", "#bcbd22", "#003f5c", "#7a5195"
  ]
  ```
  超过 12 组时扩展 `tab20` → `tab20b` → `tab20c` → `hsv`

- **group2 颜色**（`base.py:279–288`）：
  ```python
  def _global_secondary_group_color_map(secondary_groups, group_orders):
      ordered_secondary = _ordered_unique_with_order(secondary_groups, group_orders)
      ordered_colors = _hue_wheel_color_series(len(ordered_secondary))
      return ordered_secondary, {group: color for idx, (group, color) in enumerate(zip(ordered_secondary, ordered_colors))}
  ```
  使用 CIELCh 色相轮生成，`hue_start=15.0`, `lightness=65.0`

**形状映射**（`base.py:49–64`）：
  ```python
  PCA_GROUP_MARKERS = ["o", "s", "^", "D", "P", "X", "v", "<", ">", "p", "H", "8", "d", "*"]
  ```
  Plotly 对应符号：`circle`, `square`, `triangle-up`, `diamond`, `cross`, `x`, `triangle-down`, `triangle-left`, `triangle-right`, `pentagon`, `hexagon`, `hexagon2`, `diamond-tall`, `star`

**散点样式：**
- 大小：`s=42`（matplotlib）→ Plotly 中 `size=8`
- 透明度：`alpha=0.90`
- 边缘：白色，`linewidth=0.8`
- z-order：3

**置信椭圆：**
- 无 group2 时绘制 95% 置信椭圆（卡方分布 `chi2.ppf(0.95, df=2)`）
- 有 group2 时不绘制（`add_group_envelope=False`）
- 椭圆填充色：对应组颜色，alpha=0.18
- 边框：对应组颜色，1.4px

**图例：**
- 位置：图外左上角 `(1.02, 1.0)`
- 有 group2 时：先列颜色图例（group2），再列形状图例（group1）
- 超过 10 项分两列，超过 20 项分三列

**PC 轴标签：**
- 格式：`PC1 (xx.x%)` / `PC2 (xx.x%)`
- 保留 1 位小数

### 3.2 后端数据格式

`export_pca_page`（`figure_data.py:119–161`）已生成统一数据结构：

```json
{
  "figure_id": "pca",
  "title": "PCA Explorer",
  "chart_type": "scatter",
  "interactive_page_id": "pca",
  "plotly_spec": {
    "datasets": {
      "transcriptome": {
        "source": "transcriptome",
        "title": "Transcriptome PCA",
        "samples": ["S1", "S2", ...],
        "coords": [[pc1, pc2, pc3, pc4, pc5], ...],
        "var_exp": [45.2, 23.1, 12.4, 6.8, 3.2],
        "groups": [
          {"sample_id": "S1", "group1": "Control", "group2": "Batch_A"},
          ...
        ],
        "group_styles": {
          "group1": {
            "groups": ["Control", "Treatment"],
            "colors": {"Control": "#1f77b4", "Treatment": "#d62728"},
            "markers": {"Control": "circle", "Treatment": "square"}
          },
          "group2": {
            "groups": ["Batch_A", "Batch_B"],
            "colors": {"Batch_A": "#C15A3A", "Batch_B": "#3A7AC1"}
          }
        }
      },
      "metabolome": { ... }
    }
  },
  "default_state": {"source": "transcriptome", "color_by": "group1", "x_pc": 1, "y_pc": 2},
  "available_states": {
    "source": ["transcriptome", "metabolome"],
    "color_by": ["group1", "group2"],
    "x_pc": [1, 2, 3, 4, 5],
    "y_pc": [1, 2, 3, 4, 5]
  }
}
```

### 3.3 交互设计方案

#### 渲染方式
使用 **Plotly.js**（`react-plotly.js`），原因：
- 散点图的缩放、平移、悬停 tooltip 原生支持
- 置信椭圆可用 Plotly Shapes 实现
- 与现有 `InteractivePCA.tsx` 代码框架兼容

#### 页面布局

```
┌──────────────────────────────────────┬─────────────┐
│  PCA Explorer                        │  Controls   │
│  ┌────────────────────────────────┐  │  ├─ Source   │
│  │  Plotly Scatter                │  │  ├─ X axis   │
│  │  (zoom/pan/hover)              │  │  ├─ Y axis   │
│  └────────────────────────────────┘  │  └─ Color by │
│  Source: Transcriptome | Shape: g1   │             │
│  Color: g2 | X: PC1 | Y: PC2       │             │
└──────────────────────────────────────┴─────────────┘
```

#### 绘制要点

1. **分组渲染逻辑**

   当 `color_by === "group2"` 且 group2 存在时：
   - 外层循环：遍历 group2 的每个唯一值（颜色分组）
   - 内层：对每个 group2 值，绘制一个 Plotly trace
   - trace 的 `marker.color` = group2 的颜色
   - trace 的 `marker.symbol` = 该点所属 group1 的形状（数组形式，每个点可不同形状）

   当 `color_by === "group1"` 或无 group2 时：
   - 遍历 group1 的每个唯一值
   - 每个 group1 值一个 trace
   - `marker.color` = group1 颜色
   - `marker.symbol` = group1 形状
   - 同时绘制 95% 置信椭圆（Plotly Shape）

2. **悬停 Tooltip**

   ```
   <b>sample_001</b>
   group1: Control
   group2: Batch_A
   PC1: 12.345
   PC2: -3.210
   ```
   
   使用 Plotly `hovertemplate`：
   ```
   <b>%{customdata[0]}</b><br>
   group1: %{customdata[1]}<br>
   group2: %{customdata[2]}<br>
   %{xaxis.title.text}: %{x:.3f}<br>
   %{yaxis.title.text}: %{y:.3f}<extra></extra>
   ```

3. **PC 轴选择**
   - X轴和Y轴各自独立下拉选择 PC1–PC5
   - 默认 X=PC1, Y=PC2
   - 选择相同 PC 时自动将另一轴切换（如 X=Y=PC1 时 Y 自动变为 PC2）
   - 轴标签实时更新：`PC{n} ({var_exp[n-1]:.1f}%)`

4. **数据源切换**
   - 下拉选择 Transcriptome / Metabolome
   - 切换时坐标、方差解释率、分组信息全部更新
   - 但保持当前 X/Y 轴选择和 color_by 设置

5. **Color by 切换**
   - 下拉选择 group1 / group2
   - group1：颜色+形状均来自 group1，绘制置信椭圆
   - group2：颜色来自 group2，形状来自 group1，不绘制置信椭圆

6. **图例**
   - color_by=group2 时：先列 group2 颜色图例，再列 group1 形状图例
   - 图例位置：`(1.02, 1)`，左上角锚定
   - 字体大小 10px

7. **样式一致性**
   - 背景色：`#ffffff`
   - 网格线：`#e5e7eb`
   - 零线：`#cbd5e1`
   - 散点大小：8
   - 散点边缘：白色 0.8px
   - 字体：Arial, sans-serif

#### 控制面板

| 控件 | 类型 | 选项 | 说明 |
|------|------|------|------|
| Data source | select | transcriptome, metabolome | 切换数据集 |
| Color by | select | group1, group2 | 切换颜色编码 |
| X axis | select | PC1, PC2, PC3, PC4, PC5 | 横轴主成分 |
| Y axis | select | PC1, PC2, PC3, PC4, PC5 | 纵轴主成分 |

---

## 四、实现路径

### 4.1 前端修改清单

1. **`InteractiveRouter.tsx`**
   - 已添加 `"pca": InteractivePCA`

2. **`InteractivePCA.tsx`**
   - 已有 `PCAChart` / `LegacyPCAChart` 双模式
   - 需要确保新 `datasets` 格式的完整渲染逻辑
   - 需要补充 `color_by` 控制面板选项
   - 需要验证 group2 颜色 + group1 形状的混合渲染

3. **`InteractiveDendrogram.tsx`**
   - 需要重写为 D3.js 绘制
   - 实现分支悬停高亮和 tooltip
   - 添加 Y 轴刻度
   - 添加 orientation 切换

4. **`FigureViewer.tsx`**
   - 已完成 F02–F09 → `pca` 页面的 URL 参数映射

### 4.2 后端修改清单

1. **`figure_data.py`**
   - `export_pca_page`：已为 F02–F09 生成统一数据结构 ✓
   - 需要确保 correlation 分析时 `export_dendrogram` 被调用并写入 `dendrogram.json`

2. **`models.py`**
   - `FigureDataResponse` 已添加 `tree_data` 等字段 ✓

### 4.3 数据流验证

```
静态图点击 → FigureViewer.figureToInteractiveTarget()
           → buildInteractiveUrl(jobId, {pageId: "pca", params: {...}})
           → 新窗口打开 /interactive/{jobId}/pca?source=...&color_by=...
           → InteractivePageShell 读取 URL params 设置 controls.state
           → apiFetchJson(/api/jobs/{jobId}/figure-data/pca)
           → 后端返回 FigureDataResponse（含 datasets）
           → InteractivePCA 渲染 Plotly scatter
```

---

## 五、附录：静态图 vs 交互图对照

| 要素 | 静态图（matplotlib） | 交互图（Plotly/D3） |
|------|---------------------|---------------------|
| F01 聚类方法 | scipy linkage average | 同左，自实现布局 |
| F01 着色阈值 | 70% max height | 同左，可拖动调整 |
| F01 Y轴 | Euclidean Distance | 同左，带刻度 |
| F01 样本标签 | 垂直，动态字号 | 同左，SVG text |
| F02–F09 group1 | 颜色+形状 | color_by=group1 时同左 |
| F02–F09 group2 | 颜色（色相轮） | color_by=group2 时同左 |
| F02–F09 形状 | 14种marker | Plotly symbol 映射 |
| F02–F09 散点大小 | s=42 | size=8 |
| F02–F09 边缘 | white, 0.8px | 同左 |
| F02–F09 置信椭圆 | chi2 95% | 同左，Plotly Shape |
| F02–F09 PC标签 | PC1 (xx.x%) | 同左 |
| F02–F09 图例 | 图外左上 | 同左 |
| 悬停 | 无 | 显示 sample_id + group + 坐标 |
| 缩放 | 无 | 滚轮/按钮 |
| PC切换 | 无 | 下拉选择 PC1–PC5 |
| 数据源切换 | 无 | Transcriptome/Metabolome |

---

## 六、实现状态（已合并到主分支）

### 6.1 前端修改

#### `frontend/src/viewer/FigureViewer.tsx`
- ✅ `figureToInteractiveTarget()` 函数：F02–F09 全部映射到 `pca` 页面，通过 URL params 传递初始 `source` + `color_by` + `x_pc` + `y_pc`
- ✅ `buildInteractiveUrl()` 函数：将参数拼接为 `?source=...&color_by=...`
- ✅ 添加 PDF 下载按钮
- ✅ 修复 PNG 下载逻辑（统一下载 URL 构建）

#### `frontend/src/interactive/InteractiveRouter.tsx`
- ✅ 添加 `"pca"` 路由映射到 `InteractivePCA`
- ✅ 保留 `"pca-scatter"` / `"pca-pairs"` 向后兼容

#### `frontend/src/interactive/InteractivePage.tsx`
- ✅ `ControlsAPI` 接口添加 `downloadPDF` 方法
- ✅ 工具栏添加 PDF 下载按钮

#### `frontend/src/interactive/charts/InteractivePCA.tsx`
- ✅ `PCAChart` 组件：支持 `datasets` 新数据格式
- ✅ 添加 `color_by` 状态和控制面板下拉选择
- ✅ `buildPcaTraces()` 函数：根据 `color_by` 参数切换渲染模式
  - `color_by=group2`：group2 颜色 + group1 形状，无置信椭圆
  - `color_by=group1`：group1 颜色 + group1 形状 + 95% 置信椭圆
- ✅ PC 轴选择器：X/Y 各自独立下拉 PC1–PC5
- ✅ 数据源切换器：Transcriptome / Metabolome
- ✅ 悬停 tooltip：显示 sample_id + group1 + group2 + 坐标
- ✅ 保留 `LegacyPCAChart` 兼容旧 `plotly_spec` 数据格式

#### `frontend/src/interactive/charts/InteractiveDendrogram.tsx`
- ✅ `normalizeDendrogram()` 函数：支持 scipy `dendrogram` 输出和 `nodes/linkage` 树形数据
- ✅ 阈值着色：支持用户自定义阈值（滑块控制），实时重算颜色
- ✅ Y 轴刻度：Euclidean Distance 数值标签
- ✅ 阈值参考线：虚线标注当前阈值
- ✅ 悬停交互：分支悬停高亮 + tooltip 显示样本ID列表
- ✅ 控制面板：Color threshold 滑块（0 ~ 1.5×基础阈值）+ Orientation 切换
- ✅ 样本标签：垂直排列，动态字号

#### `frontend/src/App.tsx`
- ✅ `deleteJob()` 函数：修复为仅在 `res.ok` 时才从列表移除

### 6.2 后端修改

#### `backend/app/models.py`
- ✅ `FigureDataResponse` 模型：
  - 添加 `model_config = {"extra": "allow"}` 允许额外字段
  - 添加 `tree_data`、`upset_data`、`ridge_data`、`bar_data`、`circos_data` 字段
  - `available_states` 类型从 `dict[str, list[str]]` 放宽为 `dict[str, Any]`

#### `backend/app/job_execution.py`
- ✅ `_generate_deg_figure_data()` 函数：从 DEG 分析输出的 `*.all.csv` 生成 volcano 图交互数据，写入 `outputs/figure_data/volcano.json`
- ✅ `_generate_dem_figure_data()` 函数：从 DEM 分析输出的 `*.all.csv` 生成 volcano 图交互数据，写入 `outputs/figure_data/volcano.json`
- ✅ `_write_figure_data()` 辅助函数
- ✅ `_run_differential_job()` 和 `_run_dem_job()` 中调用 figure_data 生成

#### `omicsprism/src/omicsprism/visualization/figure_data.py`
- ✅ `export_pca_page()` 函数：统一 PCA 数据导出，包含 transcriptome + metabolome 双数据集的 `coords`、`var_exp`、`groups`、`group_styles`
- ✅ `export_pca_scatter()` / `export_pca_pairs()`：都委托给 `export_pca_page()`
- ✅ `export_dendrogram()` 函数：输出完整 scipy dendrogram 数据（`icoord`、`dcoord`、`ivl`、`color_list`、`branch_samples`）+ D3 树形结构（`nodes`、`linkage`）
- ✅ `EXPORT_MAP`：F02–F09 全部映射到 `"pca"` 页面

### 6.3 已知限制

1. **Orientation 切换（dendrogram）**：控制面板已添加 vertical/horizontal 下拉，但当前 SVG 渲染只实现了 vertical 布局。horizontal 布局需要额外实现坐标旋转。
2. **Pairs 矩阵（F04/F05/F08/F09）**：当前统一 PCA 页面只提供 2D 散点模式（任选 X/Y PC 轴），不提供完整的 pairs 散点矩阵。但功能上等价（用户可手动选择任意 PC 组合）。
3. **静态文件下载（PCA 页面）**：`static_files` 中只配置了 transcriptome_pca 的 png/svg 路径，下载时总是下载转录组 PCA 的静态图。如需下载代谢组 PCA 的静态图，需根据当前 source 动态选择路径。
4. **DEG/DEM volcano 页面**：当前只实现了基础 volcano 散点图，缺少阈值线交互调整。
