# OmicsPrism Platform 环境变量
# 复制此文件为 .env 并修改值

# === PostgreSQL ===
POSTGRES_PASSWORD=omicsprism_prod_2024

# === MinIO ===
MINIO_USER=minioadmin
MINIO_PASSWORD=changeme_minio_password

# === 认证（开发模式） ===
DEV_EMAIL=admin@omicsprism.local
DEV_PASSWORD=changeme

# === 日志级别 ===
LOG_LEVEL=INFO

# === 前端端口（对外暴露） ===
FRONTEND_PORT=18085

# === 前端挂载路径（宿主机 nginx 用 /api/wb/ 转发到 Docker 时保持默认） ===
PUBLIC_BASE_PATH=/api/wb/
API_BASE_PATH=/api/wb/api

# === 历史任务保留 ===
JOB_HISTORY_TTL_HOURS=24
HOUSEKEEPING_INTERVAL_SECONDS=3600
