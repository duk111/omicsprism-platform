# F14 / F21 Web 交互设计

本文档只规划实现方案，暂不改代码。设计依据来自 `omicsprism/src/omicsprism/visualization/static/distribution.py` 中的静态绘图逻辑。

## 1. 静态图逻辑确认

### F14: Top Metabolite Group1 Violin Box

静态函数：`plot_top_metabolite_group1_violin_box`

数据与分组：
- 代谢物矩阵来自 `_metabolomics_df(adata)`。
- 样本分组来自 group table 的 `group1`。
- 使用 `_align_exact_group1_to_samples()` 只保留代谢物矩阵中能精确匹配到 group table 的样本。
- `group1` 顺序用 group table 原始顺序推导：`_ordered_unique_with_order(annotation_df["group1"], _group_table_order)`。
- `group1` 颜色使用 `_group_color_map(group1_order)`。

top 代谢物选择：
- 优先从 `engine.ml_results["metabolite_summary"]["Metabolite"]` 读取候选顺序。
- 如果没有 `metabolite_summary` 或缺少 `Metabolite` 列，则按代谢物方差降序选择。
- 只保留存在于代谢物矩阵列中的代谢物。
- 静态图默认取 `top_m or 12` 个代谢物。
- Web 初始 top4 应取该静态候选顺序的前 4 个。

每个 panel：
- x 轴：`group1`
- y 轴：代谢物 abundance z-score
- title：代谢物 ID
- 图形：violin + box + strip points
- x 轴标签旋转 -35 度，即从左上到右下倾斜，y 方向网格线。

### F21: Module Eigengene Group1 Violin Box

静态函数：`plot_module_eigengene_group1_violin_box`

数据与分组：
- 模块 eigengene 来自 `engine.ml_results["module_eigengenes_df"]`，并经过 `_coerce_module_eigengene_df()`。
- 模块顺序来自 `_module_order_from_summary(module_summary_df, eigengenes_df.columns)`。
- 只保留 eigengene 矩阵中存在的模块。
- eigengene 先按模块行做 z-score：`_row_zscore(eigengenes_df.loc[:, module_order].T).T`。
- 样本分组使用 `_align_group_annotations_to_samples(samples, group_df)`。
- `group1` 顺序与颜色来自 `_module_group_orders_and_colors()`。

top 模块选择：
- 静态图按 `module_summary_df` 推导出的模块顺序展示全部模块。
- Web 初始 top4 应取该模块顺序的前 4 个。

每个 panel：
- x 轴：`group1`
- y 轴：Module eigengene z-score
- title：模块 ID
- 图形：violin + box + strip points
- x 轴标签旋转 -35 度，即从左上到右下倾斜，y 方向网格线。

## 2. 交互目标

F14 和 F21 指向同一个 `violin-box` 交互页面。F22 kME 后续单独设计，不并入 F14/F21 的 `violin-box` 页面。

页面固定展示 2x2 四个 panel：
- Left top
- Right top
- Left bottom
- Right bottom

左侧栏提供：
- `View` 下拉：仅 `Metabolite` / `Module`
- `Plot type` 下拉：`Violin only` / `Box only` / `Violin + Box`
- 四个 panel 的选择控件：
  - F14/Metabolite 视图：每个 panel 可选择一个代谢物 ID
  - F21/Module 视图：每个 panel 可选择一个模块 ID

初始状态：
- 从 F14 静态图点击进入：默认 `View = Metabolite`，四个 panel 为 F14 静态候选顺序前 4 个代谢物。
- 从 F21 静态图点击进入：默认 `View = Module`，四个 panel 为 F21 静态模块顺序前 4 个模块。
- `Plot type` 默认 `Violin + Box`，以匹配静态图主体样式。

## 3. 渲染工具选择

推荐继续使用 Plotly。

原因：
- Plotly 对 violin、box、subplot、hover 和 modebar 支持成熟。
- 2x2 panel 可以用独立 `xaxis/yaxis` domain 实现，和 F13/F25 当前交互页保持一致。
- `Plot type` 切换可以通过 trace 组合完成，不需要重新设计 canvas/SVG 渲染器。
- hover 可显示样本 ID、group1、feature ID 和数值。

注意：
- 静态图包含 strip points。用户当前只要求三种模式：仅小提琴、仅箱线、小提琴+箱线。交互图中点可作为 violin/box trace 的 `points` 展示；默认 `Violin + Box` 时保留散点以尽量接近静态图。

## 4. 后端导出设计

修改文件：
- `omicsprism/src/omicsprism/visualization/figure_exports/violin_box.py`

目标：
- F14 和 F21 分别导出同一页面 `violin-box` 的不同 view 数据。
- 依赖现有 `figure_data.py` 的 shared page merge 机制，第二个导出进入 `alt_data`。
- F22 kME 不作为本次 F14/F21 交互改造目标，不导出到这个 `violin-box` 页面；后续单独页面再实现。

建议导出结构：

```ts
{
  plotly_spec: {
    view: "metabolite" | "module";
    features: FeaturePayload[];
    feature_options: string[];
    default_feature_ids: string[]; // 长度 4，不足补空字符串
    group_order: string[];
    group_colors: string[];
    y_label: string;
  };
  default_state: {
    view: "metabolite" | "module";
    plot_type: "violin+box";
    panel_1_feature_id: string;
    panel_2_feature_id: string;
    panel_3_feature_id: string;
    panel_4_feature_id: string;
  };
  available_states: {
    view: ["metabolite"] 或 ["module"]，merge 后为 ["metabolite", "module"];
    plot_type: ["violin", "box", "violin+box"];
    feature_options: string[];
  };
}
```

`FeaturePayload`：

```ts
{
  id: string;
  rank: number;
  type: "metabolite" | "module";
  label: string;
  groups: Array<{
    group: string;
    values: number[];
    sample_ids: string[];
  }>;
}
```

F14 导出细节：
- 复用静态 `_top_metabolite_order(engine, metab_df, top_m)`。
- `top_m` 用静态默认值 12，保证 feature option 顺序与静态图一致。
- 每个 group 的 `values` 和 `sample_ids` 必须成对过滤，不能只 dropna values。

F21 导出细节：
- 复用 `_coerce_module_eigengene_df()`、`_module_order_from_summary()`、`_row_zscore()`、`_align_group_annotations_to_samples()`、`_module_group_orders_and_colors()`。
- `feature_options` 按静态模块顺序。
- 每个 group 的 `values` 来自模块 eigengene z-score。

## 5. 前端状态设计

修改文件：
- `omicsprism-platform/frontend/src/interactive/charts/InteractiveViolinBox.tsx`
- `omicsprism-platform/frontend/src/viewer/FigureViewer.tsx`
- 如需紧凑侧栏样式，可少量扩展 `frontend/src/interactive/InteractivePage.css`

前端状态：

```ts
{
  view: "metabolite" | "module";
  plot_type: "violin" | "box" | "violin+box";
  panel_1_feature_id: string;
  panel_2_feature_id: string;
  panel_3_feature_id: string;
  panel_4_feature_id: string;
}
```

视图切换：
- 像 F12/F23、F13/F25 一样，根据 `data.default_state.view` 和 `data.alt_data[*].default_state.view` 选择 effective spec。
- 切换 `View` 时，四个 panel 重置为该 view 的 `default_feature_ids[0..3]`。
- F14/F21 跳转进入同一个页面，但 URL 参数不同：
  - F14：`/interactive/{jobId}/violin-box?view=metabolite`
  - F21：`/interactive/{jobId}/violin-box?view=module`
- F22 暂不纳入这个 view 切换；若仍走 `violin-box`，需要保留兼容分支或后续单独设计。

## 6. 四宫格渲染方案

每个 panel 独立 x/y 轴：
- x：`group1`
- y：当前 feature 的 values
- title：当前 feature ID
- y label：
  - Metabolite：`Metabolite abundance z-score`
  - Module：`Module eigengene z-score`

Plot type trace 组合：
- `Violin only`
  - 每个 group 一个较宽的 `violin` trace
  - `box.visible = false`
  - `points = "all"`，样本点使用小号深色半透明点，接近静态图 strip points
- `Box only`
  - 每个 group 一个按 group1 颜色填充的 `box` trace
  - `boxpoints = "all"`，样本点同样使用对应 group1 颜色
- `Violin + Box`
  - 每个 group 一个较宽、较高不透明度的彩色 `violin` trace
  - `box.visible = true`，箱线使用对应 group1 颜色填充并叠加在小提琴中间
  - `points = "all"`，点使用对应 group1 颜色，保留静态图里的 strip points 观感
  - `meanline.visible = false`，更接近静态图的 violin + box 主体

hover：
- Sample ID
- group1
- feature ID
- value，保留 3 位小数

布局：
- 固定 2x2，初始只展示 top4。
- 每个 panel 间距略大，避免 x 轴 group label 和图形重叠。
- x 轴 group label 使用 -35 度旋转并左对齐，和更新后的 F14/F21/F22/F27/F28 静态图方向一致。
- 图例只显示一次 group1 颜色说明，放右侧或上方，避免遮挡 panel。

## 7. 左侧栏设计

顶部：
- `View` select：Metabolite / Module
- `Plot type` select：Violin only / Box only / Violin + Box

Panel controls：
- Left top：Feature select
- Right top：Feature select
- Left bottom：Feature select
- Right bottom：Feature select

Feature select 文案：
- Metabolite 视图显示 `Metabolite`
- Module 视图显示 `Module`

选择规则：
- 选择某个 feature 后，只替换对应 panel，不影响其他 panel。
- 如果 feature 无有效数据，panel 显示 `No data available`。

## 8. 需要修改的代码文件

后端：
- `omicsprism/src/omicsprism/visualization/figure_exports/violin_box.py`
  - 重构 F14/F21 导出为四宫格交互所需结构。
  - 保持 F14/F21 与静态图一致的 feature 顺序、group 顺序、颜色和 z-score 逻辑。
  - 保留或兼容 F22 kME 现有导出，避免本次修改破坏 F22。

前端：
- `omicsprism-platform/frontend/src/interactive/charts/InteractiveViolinBox.tsx`
  - 改为 2x2 panel。
  - 支持 `alt_data` 切换 view。
  - 支持四个 feature select。
  - 支持三种 plot type。
- `omicsprism-platform/frontend/src/viewer/FigureViewer.tsx`
  - F14 跳转加 `view=metabolite`。
  - F21 跳转加 `view=module`。
- `omicsprism-platform/frontend/src/interactive/InteractivePage.css`
  - 如需要，增加 violin 四宫格侧栏样式，尽量复用 F13/F25 的 panel control 样式。

## 9. 验收标准

F14：
- 从 F14 静态图打开后进入同一个 `violin-box` 页面，默认 view 为 Metabolite。
- 初始四个 panel 是 F14 静态 top 代谢物顺序的前四个。
- group1 顺序、颜色、y 值来源与静态图一致。
- 每个 panel 可独立换代谢物 ID。

F21：
- 从 F21 静态图打开后进入同一个 `violin-box` 页面，默认 view 为 Module。
- 初始四个 panel 是 F21 静态模块顺序的前四个。
- module eigengene z-score、group1 顺序、颜色与静态图一致。
- 每个 panel 可独立换模块 ID。

通用：
- 左侧 `View` 可在 Metabolite / Module 之间切换，并重置四个 panel 为对应 view 的 top4。
- `Plot type` 可切换仅小提琴、仅箱线、小提琴+箱线。
- hover 能显示 sample ID。
- 页面固定展示 top4 四宫格，不再一次性铺满所有 feature。
- 不影响静态图生成。
